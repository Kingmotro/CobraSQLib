package us.drome.cobrasqlib;

/**
 * A basic enum that allows an easy way to set valid column definition options.
 * 
 * @author TheAcademician
 * @since 0.1
 */
public enum Options {
    isPrimary, isAutoincrement, isNotNull, isUnsigned;
}